#define MAXHEIGHT 1000
#define MAX 100

typedef int Array2D[MAX][MAX];

void ReadFromFile(Array2D a, int &h);

int Solve(Array2D f, Array2D a, int h);

void Results(Array2D a, Array2D f, int h);

